#include <stdio.h>

int main(){
	char ch;
	int it;
	long int lit;
	float ft;
	double dft;
	long double ldft;

	printf("%d, %d, %d, %d, %d, %d\n", sizeof(ch), sizeof(it), sizeof(lit), sizeof(ft), sizeof(dft), sizeof(ldft));
}
